docker create -i -t --volume `pwd`:/workspace -w /workspace --name "rails_devcontainer" ruby:3.1
docker start rails_devcontainer
docker exec -i -t -w /workspace rails_devcontainer /bin/bash -l

bundle exec rails server --binding 0.0.0.0


puma.db: bind "tcp://#{ENV.fetch('INTERFACE', '127.0.0.1')}:#{ENV.fetch('PORT', '3000')}"

docker compose exec app /bin/bash -l

alias be="bundle exec"

docker compose up -d --build
docker compose up -d
docker compose exec app /bin/bash -l
docker compose down

docker-compose -f .devcontainer/docker-compose.yml -p project_name up
